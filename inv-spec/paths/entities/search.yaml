get:
  summary: Search entities
  parameters:
    - name: search
      in: query
      description: A title, author, or ISBN
      required: true
      type: string
      x-example: Les Misérables
    - $ref: ../partials/lang.yaml
  tags:
    - Entities
