get:
  summary: Last nearby items
  description: |
    **Requires to be [authentified](/#!Auth/post_auth_action_login)**<br>
    Based on the logged in user position
  parameters:
    - $ref: ../partials/limit.yaml
    - $ref: ../partials/offset.yaml
  tags:
    - Items

  responses:
    '200':
      $ref: ./partials/200.yaml
